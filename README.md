# CSV-Data-Cleaning
A Data Analytics minor project focused on cleaning and preprocessing an employee CSV dataset using Python, Pandas, and NumPy. The project handles missing values, invalid and infinite values, negative values, duplicate records, and inconsistent data to produce a cleaner dataset suitable for further analysis.
